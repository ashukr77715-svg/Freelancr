# Build tools (TypeScript, @types/*, Vite, etc.) live in devDependencies but
# are required to build. NODE_ENV=production would otherwise skip them, so
# force dev deps to install. They aren't shipped to the running process.
include=dev
